# This file will never be updated with any significant functionality, so it's
# safe to update with your info, and then ignore updates forever.
#
# 1. Update your info below
# 2. Run this in your terminal:
#       git update-index --assume-unchanged ~/Dotfiles/Git/config/user

[user]
  name = Your Name (edit in ~/Dotfiles/Git/config/user)
  email = your.email@example.com
