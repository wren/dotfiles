-- Keymap --
-- The default mapping for sandwich (`s`) override a default binding, and that
-- causes issues with some more obstinate plugins (i.e. vim-visual-multi)
cmd 'runtime macros/sandwich/keymap/surround.vim'

