- name: Bootstrap desktop environment
  hosts: local
  vars:
    # general (probably don't edit)
    home: "{{ '~' | expanduser }}"
    config_home: "{{ home }}/.config"
    data_home: "{{ home }}/.local/share"
    cache_home: "{{ home }}/.cache"
    bin_home: "{{ home }}/.local/bin"

    # personal info (edit here)
    dotfiles_home: "{{ home }}/Dotfiles"
    full_name: Jonathan Wren
    computer_user: jonathan
    github_user_name: wren
    email: jonathan@nowandwren.com
  roles:
    - role: spali.beautiful_output
    - {role: bootstrap, tags: bootstrap}
    - {role: encryption, tags: encryption}
    - {role: 1password, tags: 1password}
    - {role: age, tags: age}
    - {role: asdf, tags: asdf}
    - {role: bat, tags: cli}
    - {role: discord, tags: discord}
    - {role: doppler, tags: doppler}
    - {role: fira-code-nerd-font, tags: fonts}
    - {role: monolisa-nerd-font, tags: fonts}
    - {role: git, tags: git}
    - {role: jrnl, tags: jrnl}
    - {role: less, tags: cli}
    - {role: lolcat, tags: cli}
    - {role: neovim, tags: editor}
    - {role: python, tags: python}
    - {role: ripgrep, tags: cli}
    - {role: wezterm, tags: terminal}
    - {role: zoom, tags: zoom}
    - {role: zoxide, tags: cli}
    - {role: viu, tags: cli}
    - {role: zsh, tags: cli}
