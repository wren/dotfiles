- brew:
    - "direnv" # Load/unload environment variables based on $PWD

- create:
    - ~/.config/direnv

- link:
    ~/.config/direnv:
        glob: true
        path: '*'
        exclude:
          - install.conf.yaml
